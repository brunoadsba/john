# FastAPI e servidor
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# IA - Speech to Text (opcional - pode falhar se FFmpeg não estiver instalado)
faster-whisper>=1.0.0  # Requer FFmpeg instalado no sistema
# openai-whisper>=20231117  # Alternativa ao faster-whisper

# IA - LLM (opcional)
ollama>=0.1.6
groq>=0.4.2

# IA - Text to Speech
edge-tts>=6.1.0  # Fallback funcional com vozes naturais pt-BR
# piper-tts>=1.2.0  # Opcional - pode ter problemas de instalação

# IA - Wake Word Detection (ESSENCIAL - Open Source)
openwakeword>=0.5.0

# Utilidades
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Banco de dados
aiosqlite>=0.19.0  # SQLite assíncrono para Python

# Áudio
numpy>=1.26.3
soundfile>=0.12.1
pydub>=0.25.1  # Para conversão de formatos de áudio (MP3 → WAV)
# librosa>=0.10.1  # Opcional - pode ter dependências do sistema

# Logging e monitoramento
loguru>=0.7.2

# Testes
pytest>=7.4.4
pytest-asyncio>=0.23.3
httpx>=0.26.0

# Rate Limiting
slowapi>=0.1.9

# Busca Semântica (Opcional - melhora recuperação de memórias)
sentence-transformers>=2.5.1
torch>=2.0.0  # Requerido pelo sentence-transformers (~2GB)

# Busca Web (Tool Calling)
duckduckgo-search>=4.1.0  # Busca web gratuita (padrão)
tavily-python>=0.3.0  # Opcional - busca web premium (requer API key)

# Fine-tuning e RLHF
peft>=0.6.0  # Para LoRA (Low-Rank Adaptation)
transformers>=4.35.0  # Para carregar modelos
accelerate>=0.24.0  # Para aceleração de treinamento
datasets>=2.14.0  # Para gerenciar datasets
trl>=0.7.0  # Para PPO (Reinforcement Learning)

# Clustering e Análise
scikit-learn>=1.3.0  # Para clustering de intenções
pandas>=2.0.0  # Para análise de dados

# Cache (Otimização de Performance - Fase 1)
cachetools>=5.3.0  # Cache TTL para TTS e respostas

# Dependências para melhor performance (opcional)
# onnxruntime>=1.0.0  # Já incluído com openwakeword
