name: jonh_assistant
description: Assistente de voz local Jonh
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  
  # Audio
  record: ^5.0.4
  just_audio: ^0.9.36
  permission_handler: ^11.0.1
  
  # Network
  web_socket_channel: ^2.4.0
  http: ^1.1.2
  
  # State Management
  provider: ^6.1.1
  
  # Wake Word (Porcupine)
  # picovoice_flutter: ^3.0.0  # Descomentar quando configurar
  
  # Utils
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  # assets:
  #   - assets/images/
  #   - assets/sounds/

